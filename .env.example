# ============================================================
# SILA STUDIOS — Environment Variables
# Copy this file to .env.local and fill in the values
# NEVER commit .env.local to GitHub
# ============================================================

# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://zurjdutxqjyletqjjkjx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# Admin Auth
# Generate this with: node scripts/generate-hash.js yourpassword
ADMIN_PASSWORD_HASH=your_bcrypt_hash_here

# JWT Secret — any long random string, keep it secret
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_jwt_secret_here
